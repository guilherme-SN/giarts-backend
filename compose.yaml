services:
  mysql:
    image: 'mysql:latest'
    container_name: mysql_db
    environment:
      - 'MYSQL_DATABASE=giartsdatabase'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306'
